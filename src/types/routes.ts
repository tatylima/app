export enum RoutePath {
    HOME = '/',
    LOGIN = '/login',
    SETTINGS = '/settings',
    SETTINGS_PRODUCTS = 'products',
    SETTINGS_GAMES = 'games',
    SETTINGS_USERS = 'users',
  }

  /**criando um arquivo com nomes da rotas
   * exportando o caminho das rotas
   * enum (constantes enomeadas)**/
